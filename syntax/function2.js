ex_round = Math.round(3.14)
console.log(ex_round)
ex_round2 = Math.round(3.64)
console.log(ex_round2)

function sum(frist, second) { //매개체들 parameter(매개변수)

    return frist + second;
}

console.log(sum(2, 4)); // 입력값들을 argument (인자)